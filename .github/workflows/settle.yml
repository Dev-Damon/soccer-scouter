name: 베팅 자동 정산
on:
  schedule:
    - cron: '*/20 * * * *'   # 20분마다 — 종료된 경기 베팅 자동 정산(공개 anon RPC, 시크릿 불필요)
  workflow_dispatch: {}
permissions:
  contents: read
concurrency:
  group: bet-settle
  cancel-in-progress: false
jobs:
  settle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      # 배당 동기화 — match_odds를 화면(betOddsOf)과 동일 공식으로 채움. 녹아웃 라운드 확정 시 배당 누락(→2.0 기본값) 방지.
      - name: 배당 동기화 (match_odds)
        env:
          ODDS_SYNC_SECRET: ${{ secrets.ODDS_SYNC_SECRET }}
        run: node scripts/sync_odds.mjs
      # 게이트 — 라이브/최근 종료 경기 없으면 정산 스킵(헛돌지 않게)
      - name: 라이브/최근경기 확인
        id: gate
        run: |
          if node scripts/has_active.mjs; then echo "active=1" >> "$GITHUB_OUTPUT"; else echo "active=0" >> "$GITHUB_OUTPUT"; fi
      - name: 종료 경기 베팅 정산
        if: steps.gate.outputs.active == '1'
        run: node scripts/settle.js
